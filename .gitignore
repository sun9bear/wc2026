# 依赖
node_modules/
.pnp
.pnp.*

# Next.js / 构建产物
.next/
out/
build/
dist/
*.tsbuildinfo

# 环境变量 / 密钥
.env
.env.local
.env*.local

# 日志
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# 部署
.vercel

# 编辑器 / 操作系统
.DS_Store
Thumbs.db
.vscode/
.idea/

# 本地工具数据
.claude/

# 机密（凭据/密钥，绝不提交）
docs/secret/
.gstack/

# CodeX 数据收集 WIP（H5 游戏用，非运行时）。根锚定 / —— 只忽略根目录这两个，
# 绝不可写成无斜杠的 data/（那会连被 app 导入的 src/data/ 一起忽略 → 构建挂）。
# Vercel CLI 部署遵守 .gitignore（无 .vercelignore 时）→ 这两项下次部署也不再上传。
/data/
/logo2.png
